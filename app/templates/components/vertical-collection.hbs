{{#each content key=keyForId as |item|}}
  {{#vertical-item
    defaultHeight=defaultHeight
    tagName=itemTagName
    content=item
    alwaysUseDefaultHeight=alwaysUseDefaultHeight
    keyForId=keyForId
    collection=this
    as |wrapper|
  }}
    {{#if wrapper.contentInserted}}
      {{yield item}}
    {{/if}}
  {{/vertical-item}}
{{/each}}