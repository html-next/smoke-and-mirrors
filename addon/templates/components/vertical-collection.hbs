{{#each _content key="@identity" as |item index|}}
  {{#vertical-item
    defaultHeight=defaultHeight
    itemTagName=itemTagName
    content=item
    index=index
    alwaysUseDefaultHeight=alwaysUseDefaultHeight
    keyForId=keyForId
    collection=this
    as |wrapper|
  }}
    {{#if shouldRenderList}}
      {{#if wrapper.contentInserted}}
        {{yield item}}
      {{/if}}
    {{/if}}
  {{/vertical-item}}
{{/each}}