import { element } from 'dummy/helpers/element';
import { module, test } from 'qunit';

module('Unit | Helper | element');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = element([42]);
  assert.ok(result);
});
